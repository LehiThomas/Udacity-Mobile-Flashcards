import { StyleSheet, Dimensions } from "react-native";

export default StyleSheet.create({
  buttonStyle: {
    backgroundColor: "black",
    borderRadius: 15,
    borderWidth: 0,
    width: 220,
    alignItems: "center",
    padding: 5,
    marginTop: 10
  },
  buttonText: {
    color: "white",
    fontSize: 30
  }
});
